import 'package:flutter/material.dart';

class EmptyTable extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Пока нет ни одного сотрудников'));
  }
}